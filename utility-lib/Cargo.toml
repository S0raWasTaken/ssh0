[package]
name = "libssh0"
version = "1.0.0"
edition = "2024"

[dependencies]
chrono = "0.4.44"
crossterm = "0.29.0"
tokio = { version = "1.49.0", features = ["full"], optional = true }

[features]
tokio = ["dep:tokio"]

[lints]
workspace = true
